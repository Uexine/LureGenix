version: '3.8'

services:
  frontend:
    image: nginx:alpine
    container_name: luregenix-frontend
    ports:
      - "8080:80"
    volumes:
      - ./frontend:/usr/share/nginx/html
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    restart: unless-stopped